# ProNavigator 需求规格说明书

> 本文件汇总本轮对话中用户提出的所有需求与约束。

## 1. 项目概述

**项目名称**：ProNavigator

**定位**：公司内部导航站，管理员维护分类与网站，访客无需登录即可公开访问。

**技术栈**：Node.js + Express 前后端一体式，SQLite 数据库，Docker 部署，GitHub Actions 构建镜像。

**前端 UI 框架**：腾讯 TDesign + Vue（管理后台及公开页面均使用 TDesign + Vue 构建）。

## 2. 用户角色

- **管理员**：唯一后台用户，负责分类管理、网站管理、拖拽排序。
- **访客**：无需登录，访问公开首页及网站详情页。

## 3. 核心功能需求

### 3.1 分类管理

- 支持新建分类、编辑分类、删除分类。
- 分类名称必填且全系统唯一（不区分大小写）。
- 删除分类前校验：若分类下存在网站，禁止删除。
- 支持拖拽调整分类展示顺序。
- 拖拽排序无需确认，松手后立即生效。
- 排序调整后，所有分类的展示索引按当前顺序从 1 开始重新编号。

### 3.2 网站管理

- 支持新增网站、编辑网站、删除网站。
- 网站字段：网站名称、URL、Logo、说明、所属分类。
- URL 必须是以 `http://` 或 `https://` 开头的合法地址。
- Logo 可选上传，上传时必须为图片格式。
- 未上传 Logo 时使用默认占位图。
- 编辑网站时可以修改所属分类，保存后网站移动到目标分类。
- 网站移动到目标分类后，默认排在目标分类最后。
- 说明（描述）使用 Markdown 编辑，详情页展示渲染后的 Markdown 内容。
- Markdown 渲染时必须过滤危险标签与脚本，防止 XSS。

### 3.3 拖拽排序

- 分类和网站均支持拖拽排序。
- 拖拽排序即时生效，无需二次确认。
- 排序范围内的所有记录按当前顺序从 1 开始重新编号。
- 排序页面禁止跨分类拖拽网站；跨分类调整必须通过网站编辑完成。

### 3.4 公开首页

- 访客无需登录即可访问首页。
- 首页按分类顺序展示分类及对应网站列表。
- 每个网站卡片展示 Logo（或默认占位图）、网站名称、详情入口。
- 点击网站卡片在新标签页打开目标 URL。
- 提供详情页入口，详情页展示渲染后的 Markdown 说明。
- 无数据时展示空状态提示。

### 3.5 管理员认证

- 单管理员账号。
- 登录与权限校验必须使用 JWT Token，禁止使用 Session 或 Cookie。
- 登录成功后返回 JWT Token，前端将 Token 存储在 `localStorage`。
- 跳转到其他管理后台页面时，从 `localStorage` 读取 Token 并携带在 `Authorization: Bearer <token>` 请求头中。
- 登出时清除 `localStorage` 中的 Token。
- 管理员密码支持修改。

### 3.6 默认管理员账号

- 默认用户名：`admin`
- 默认密码：`admin123`
- 首次登录后建议立即修改密码。

## 4. 数据持久化

- 数据库类型：SQLite。
- 数据表：管理员表、分类表、网站表、审计日志表。
- Docker 部署时必须支持数据库文件持久化挂载。
- Logo 上传文件也需要持久化挂载。

## 5. 部署与构建

- 采用 Docker 部署。
- 提供 Dockerfile 与 docker-compose.yml。
- README 中需包含项目介绍、本地运行说明、Docker 部署说明、默认账号信息。
- Docker 镜像构建完全托管给 GitHub Actions 流水线。
- 目标平台仅限 `linux/amd64`，不考虑 32 位，不考虑 ARM。

## 6. 安全与约束

- JWT 签名密钥通过环境变量注入，禁止硬编码。
- Markdown 渲染使用白名单过滤危险内容。
- Logo 上传限制文件类型与大小。
- 管理操作记录审计日志。